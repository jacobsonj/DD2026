// strings represent texutal data using quotes or backticks. "s" 's' or `s`
let first = "John";
let last = "Ibis";

//Usingn the backtic with variables noted by ${} is called template literals. 
let full = `the full name is ${first} ${last}`;
console.log(full.toUpperCase()); //THE FULL NAME IS JOHN IBIS